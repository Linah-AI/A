# أركيدو (Arcado) — ملف التسليم التقني الشامل (Handoff)

> آخر تحديث: 2026-06-27 · الفرع: `claude/elegant-euler-8X2Ia` · المستودع: `Linah-AI/A`
> هذا الملف هو "الذاكرة الكاملة" للمشروع. اقرأه أولاً في أي شات جديد قبل أي عمل.

---

## 0) ملخّص في سطرين
**أركيدو** منصة ألعاب سهرة عربية تحاكي برامج المسابقات التلفزيونية، تُباع كمنتج رقمي (License Key عبر سلة/Salla). فيها 4 ألعاب، كل لعبة على **3 شاشات منفصلة**: التلفزيون (عام) + المضيف (سرّي) + الجوال (اللاعب). لعبة واحدة فقط (**من سيربح الجائزة**) مبنية ومنشورة وتعمل بالوقت الفعلي؛ الباقي شاشات "قريباً".

---

## 1) المنتج والرؤية
- **الاسم:** أركيدو (Arcado).
- **الفكرة:** ألعاب جماعية للسهرات العائلية، محاكاة لبرامج التلفزيون داخل البيت.
- **الألعاب الأربع:**
  1. **من سيربح الجائزة** (millionaire) — ✅ مبنية بالكامل وحيّة.
  2. **حروف** (huroof) — شبكة حروف 5×5 (خلية العسل سابقاً، أُعيد بناؤها كشبكة منتظمة). 🔜 شاشات قريباً + مكاب ستاتيك قديم.
  3. **طيف** (taif) — عدّاد سرعة (speedometer) بألوان مسطّحة وإبرة سميكة. 🛠️ **قيد التحويل للوقت الفعلي** — الخلفية (محرّك + بنك + أحداث سوكِت) جاهزة ومُختبرة؛ الشاشات الحيّة قيد البناء.
  4. **ديكربتو** (decrypto) — لعبة الشيفرات. 🔜
- **نموذج البيع:** منتج رقمي على منصة **سلة (Salla)**. المشتري يحصل على **License Key**. هذا المفتاح سيصير لاحقاً `playerId` في النظام (الذاكرة الدائمة مبنية على هذا الأساس بالفعل).
- **الجمهور:** عربي. كل الواجهات عربية RTL.

---

## 2) الهوية البصرية (قواعد صارمة — لا تكسرها)
أسلوب **Pop-Comic / Neo-Brutalist** (كرتوني مرِح بحدود سميكة وظلال صلبة):
- متغيّرات CSS الأساسية (في `public/css/pop.css`):
  - `--bw: 3.5px` (حد عادي) · `--bw-lg: 5px` (حد عريض) · `--shadow: 7px 7px 0 var(--ink)` (ظل صلب بلا blur).
  - خلفيات منقّطة (polka-dot).
- **الخط:** **Baloo Bhaijaan 2** (محلي، في `public/fonts/`، أوزان 500/600/700/800) لكل النصوص **والعناوين معاً**. خط ثانوي: **Changa** (700/800). الخطوط محلية تعمل بلا إنترنت (`public/css/fonts.css`).
- 🚫 **خط Tajawal محذوف نهائياً** — لا يُستخدم في أي صفحة، إطلاقاً. (المستخدم كرهه صراحةً.)
- 🚫 **لا إيموجيات إطلاقاً** في الواجهات — كل الأيقونات **SVG** (مثل `i-bolt`, `i-coin`, `i-trophy`, `i-scan`, `i-check`, `i-gauge`, `i-hex`, `i-decoder`, `i-confetti`, `i-cart`, `i-star`). (الإيموجيات في الكود/الـlog فقط مقبولة، لكن ليس في الواجهة.)
- 🔢 **أرقام إنجليزية فقط** (0-9) — لا أرقام هندية إطلاقاً.
- **الشعار (مهم):** `#arcado-mark` = **4 دوائر + نجمة** (symbol معرّف داخل SVG). معتمد في كل الصفحات. في `host.html` الشعار رابط يعود للصفحة الرئيسية.
- **تنوّع بصري مطلوب:** المستخدم رفض التوحيد الممل بين الألعاب — كل لعبة يجب أن يكون لها طابع بصري مميّز (ضمن نفس الهوية).
- ⚠️ **درس مكتسب:** المستخدم رفض بشدّة بناء تصميم "مسرح/Stage" لم يطلبه (استبدلت `index.html` بالخطأ مرة). **لا تبنِ/تستبدل تصاميم لم تُطلب صراحةً.**

---

## 3) المعمارية التقنية
- **Backend:** Node.js (ESM، `"type":"module"`، `engines.node >= 20`) + **Express** (static + `/healthz`) + **Socket.IO** (الوقت الفعلي).
- **مكتبات:** `qrcode` (باركود QR حقيقي)، `nanoid` (رموز جلسات).
- **Frontend:** HTML/CSS/JS خام **بدون أي خطوة بناء (no build step)**. كل صفحة تحمّل `/socket.io/socket.io.js` ثم سكربت الوحدة (module).
  - ⚠️ **درس:** الصفحات لم تعمل في البداية حتى أضفنا `<script src="/socket.io/socket.io.js"></script>` **قبل** سكربتات الـmodule.
- **آلة الحالة (Phase):** `lobby → question → buzzed → stolen → reveal → over`.
- **محرّك اللعبة منفصل عن الشبكة** (`server/millionaire.js` آلة حالة خالصة قابلة للاختبار، لا تعرف شيئاً عن السوكِت).

---

## 4) خريطة الملفات (دقيقة — حيّ مقابل قديم)

### ✅ الملفات الحيّة (تعمل فعلاً مع السيرفر)
| الملف | الدور |
|------|------|
| `server/index.js` | السيرفر: Express + Socket.IO، كلاس `Session`، كل أحداث السوكِت، `/healthz`. |
| `server/millionaire.js` | محرّك "من سيربح الجائزة" (آلة حالة خالصة). |
| `server/qbank.js` | بنك الأسئلة الذكي: `qid()` (FNV-1a hash)، `QuestionBank`، `roundSize`، `selectRound`، `millionaireBank`. |
| `server/taif.js` | محرّك "طيف" (آلة حالة خالصة، Wavelength). مراحل: `lobby→clue→guess→intercept→reveal→over`. هدف سرّي 0-100، تهديف بالنطاقات، الهدف يُكشف للوسيط فقط عبر `snapshot({forMedium})`. |
| `data/spectrums-taif.json` | بنك بطاقات طيف: 64 بطاقة `{left, right}` (أقطاب متضادة). الهدف يولّده السيرفر عشوائياً، البطاقة تحدّد القطبين فقط. |
| `server/progress.js` | الذاكرة الدائمة لكل لاعب: `getProgress/saveProgress/recordAnswer/stats` → `data/progress.json`. |
| `data/questions-millionaire.json` | **500 سؤال** = 5 أقسام × 100 (تاريخ، علوم، ثقافة عامة، تقنية، فن وترفيه). صيغة كل سؤال: `{cat, q, options:[4], answer:index}`. |
| `public/index.html` | الصفحة الرئيسية (pop-comic). تربط: من سيربح → `tv.html`، الباقي → `soon.html?g=الاسم`. تحتوي تعريفات كل أيقونات SVG و`#arcado-mark`. |
| `public/host.html` | **شاشة المضيف الحيّة** (سرّية، يرى الإجابات، جهاز مستقل عن التلفزيون). تلتحق بجلسة قائمة (`host:claim` بالرمز أو بمسح باركودها)، تعرض الآلة الحاسبة وأزرار الحكم. لا تُنشئ الجلسة. |
| `public/tv.html` | **شاشة التلفزيون الحيّة** (عامة، لا تكشف الإجابة قبل reveal). هي من **تُنشئ الجلسة** (`tv:create`) وتبقى على الجهاز الذي فُتحت اللعبة منه؛ تعرض غرفة انتظار بثلاث باركودات (مضيف/أخضر/أحمر) حتى ينضم الجميع. |
| `public/play.html` | **شاشة اللاعب الحيّة** (الجوال، زر Buzz). تُفتح بمسح الباركود `play.html?s=CODE&t=green|red`. |
| `public/soon.html` | صفحة "قريباً" (شعار + مخروط تحذير SVG + "على نار هادئة"). للألعاب غير الجاهزة. |
| `public/css/pop.css` | ستايل الهوية الأساسي (pop-comic). |
| `public/css/fonts.css` | تعريفات `@font-face` المحلية (Baloo + Changa، بلا Tajawal). |
| `public/fonts/*.ttf` | الخطوط المحلية + `OFL.txt` (الترخيص). |
| `render.yaml` | إعداد نشر Render. |
| `DEPLOY.md`, `README.md` | توثيق. |

### 🗄️ ملفات قديمة / مكابات ستاتيك (غير مربوطة بالسيرفر — لا يشير لها أي ملف حيّ)
`millionaire-host.html`, `millionaire-tv.html`, `millionaire-play.html`, `huroof-*.html`, `taif-*.html`, `decrypto-*.html`, `arcado.html`، وملفات CSS/JS: `experience.css`, `games.css`, `theme.css`, `js/arcado-client.js`, `js/demo.js`, `js/experience.js`.
> هذه بقايا المرحلة الأولى (مكابات ثابتة قبل بناء النسخة الحيّة). **التلفزيون/المضيف/اللاعب الحيّة هي بلا بادئة:** `tv.html` / `host.html` / `play.html` فقط. يمكن تنظيفها لاحقاً لكنها لا تضرّ.

---

## 5) تدفّق اللعب (من سيربح الجائزة)
1. **التلفزيون** (الجهاز المربوط بالشاشة الكبيرة) يفتح `tv.html` ويضغط "ابدأ جلسة جديدة" → `tv:create` → السيرفر يولّد رمز 5 أحرف + 3 باركودات (مضيف/أخضر/أحمر). تبقى الشاشة في غرفة انتظار تعرض الرمز + الباركودات + عدد المتصلين بكل فريق.
2. **المضيف** يمسح باركوده الخاص من جهازه (جوال/لابتوب آخر) أو يفتح `host.html` ويكتب الرمز يدوياً → `host:claim` → ينضم كمتحكّم بالجلسة (وحده يرى الإجابات).
3. اللاعبون يمسحون باركود فريقهم → `play.html?s=CODE&t=green|red` → `player:join`.
4. المضيف يضبط: نوع الجائزة (عيدية/هدية)، سقف العيدية (يختفي لو هدية)، **عدد لاعبي الفريق** (يفعّل الآلة الحاسبة)، طريقة الانتقال بين الأسئلة (يدوي/آلي)، ثم يبدأ → `host:start`. عندها تتحوّل شاشة التلفزيون من غرفة الانتظار إلى المسرح تلقائياً.
5. يُعرض السؤال + عدّاد. الأسرع ضغطاً على Buzz يُقبل (`player:buzz`) → الباقي يُقفل لهذا السؤال.
6. المضيف يحكم (`host:judge {correct, steal}`): صح → نقاط؛ خطأ → السؤال ينتقل **فوراً** للفريق الآخر (`stolen`، نفس لحظة الحكم، بلا زر وسيط)، والمضيف يختار من جهته "سيجيب" (يبدأ العدّاد، تُفعَّل أزرار صح/خطأ من جديد) أو "يمرر" (السؤال يحترق مباشرة). إن أخطأ الفريقان يحترق السؤال (`burned`).
7. `host:next` للسؤال التالي (يدوي)، أو ينتقل تلقائياً بعد 4 ثوانٍ من `reveal` إذا اختار المضيف الوضع الآلي. يتكرر حتى تنتهي الجولة → `over` + الفائز (مع صياغة عربية صحيحة لعدد الصناديق: بصندوق/بصندوقين/بـ3-10 صناديق/بـ11+ صندوق).
- الجلسة تموت بانفصال المضيف (`disconnect` → `session:ended`).

---

## 5.5) لعبة طيف (Wavelength) — المعمارية والتدفّق
**القرار المعماري:** السيرفر صار متعدّد الألعاب. `tv:create({game:'taif'})` يبني `TaifGame` بدل المليونير. الباركودات تشير لصفحات اللعبة (`PAGES` في `index.js`): طيف → `taif-host.html` / `taif-play.html`.

**الأدوار (قرار المستخدم):**
- **الوسيط** = لاعب من الفريق الأساسي (دور **دائري** يدور بين لاعبي الفريق كل جولة، عبر `mediumPtr` في `Session`). جواله **فقط** يرى الهدف السرّي (نفس فكرة `forHost` لكن `forMedium` — يُرسل الهدف لمقبس الوسيط وحده).
- **القائد** = بقية الفريق الأساسي (عدا الوسيط) — يحرّكون المؤشر ويثبّتون.
- **الخصم** = الفريق الآخر — يخمّن اتجاه الهدف (يمين/يسار المؤشر).
- **المضيف** = مقدّم محايد فقط: يبدأ، ينتقل بين الجولات، يدير المؤقّت. **لا يرى الهدف** (التهديف آلي في المحرّك).

**التدفّق:** `lobby → clue → guess → intercept → reveal → over`.
1. `taif:start{rounds}` (المضيف) → هدف سرّي 0-100 + بطاقة + تعيين وسيط الأخضر.
2. الوسيط يرى الهدف ويرسل `taif:clue{text}` → مرحلة `guess`.
3. القائد يحرّك المؤشر: `taif:needle{pos}` (حدث خفيف مباشر، بثّ `taif:needle{needle}` للغرفة بلا لقطة كاملة) ثم `taif:lock` → مرحلة `intercept`.
4. الخصم `taif:guess{dir:'left'|'right'}` → كشف وتهديف وبثّ `taif:result`.
5. `taif:next` (المضيف) → الأدوار تنقلب (الفريق الآخر يصير الأساسي)، وسيط جديد → حتى `over`.

**التهديف:** المسافة بين المؤشر والهدف: ≤5 = 4 نقاط، ≤12 = 3، ≤20 = 2، غير ذلك = 0. الخصم ينال نقطة اعتراض واحدة إن خمّن الاتجاه صح **و** لم يُصب الأساسي المركز (4 نقاط). 6 جولات افتراضياً (3 لكل فريق).

**حُرّاس الأمان:** السيرفر يتحقق أن مرسل `taif:clue` هو `mediumId` فقط، أن محرّك المؤشر من الفريق الأساسي وليس الوسيط، أن المخمّن من الفريق الخصم. كل أحداث طيف محميّة بـ`gameType==='taif'` وأحداث المليونير بـ`'millionaire'`.

---

## 6) بنك الأسئلة الذكي (4 متطلبات طلبها المستخدم — كلها مُنفّذة ومُختبرة)
1. **توزيع متساوٍ:** كل جولة تأخذ عدداً متساوياً من كل قسم (`selectRound` يدور على كل الأقسام).
2. **آلة حاسبة:** `perCat = round(عدد لاعبي أكبر فريق × 1)`، والإجمالي = `perCat × عدد الأقسام`. أي **كل لاعب يضيف سؤالاً واحداً من كل قسم**.
   - أمثلة (5 أقسام): لاعبان=10، 5 لاعبين=25، 10 لاعبين=50.
   - ⚠️ **درس:** القسمة بالـceil كانت خشنة (2 و5 لاعبين يعطيان نفس العدد) → صُحّحت إلى `round(players × perCatPerPlayer)`.
   - الأساس (`basis`) = `teamSize` الذي يحدّده المضيف، وإلا أكبر فريق متصل فعلياً.
3. **ذاكرة لكل لاعب (لا تكرار):** `progress.seen` (Set من qid) — كل جلسة تجلب أسئلة جديدة حتى يُستنفد البنك (500).
4. **أولوية المراجعة:** عند إعادة تدوير قسم نفد، الأسئلة المُجاب عنها **خطأً** (`progress.wrong`) تظهر أولاً، ثم يُمسح `seen` لهذا القسم لبدء دورة جديدة.
- **مُعرّف السؤال** `qid` = FNV-1a hash من `cat|q` — مستقر بين الجلسات (لا يتغيّر).
- التخزين: `data/progress.json` (مُهمل في git عبر `.gitignore`)، كتابة مؤجّلة (debounce 400ms)، المفتاح `playerId` (= License Key مستقبلاً، الآن قيمة ثابتة `'guest'` مشتركة لكل جلسات هذا السيرفر — **ليست** مرتبطة برمز الجلسة).

---

## 7) النشر (Deployment)
- **المنصة:** Render.com (Git Provider). `render.yaml`: runtime=node, plan=**free**, build=`npm install`, start=`npm start`, healthCheckPath=`/healthz`, NODE_VERSION=20.
- **الرابط الحيّ:** `https://a-50vj.onrender.com`
- **يربط تلقائياً بالـGit:** أي push للفرع → Render يعيد النشر خلال 2-4 دقائق.
- ⚠️ **سلوك الخطة المجانية:** تنام بعد ~15 دقيقة خمول؛ أول طلب بعدها يأخذ 30-50 ثانية ويظهر "Not Found" مؤقتاً ثم يصحى. **هذا طبيعي.** للإنتاج: يُنصح بخطة مدفوعة.

---

## 8) Git وسير العمل
- **الفرع الوحيد للتطوير:** `claude/elegant-euler-8X2Ia` (لا تدفع لغيره بلا إذن صريح).
- **المستودع:** `Linah-AI/A`.
- ⚠️ **البروكسي يمنع الدفع بدون توكن من المستخدم.** الدفع يتم عبر:
  `git push https://Linah-AI:<TOKEN>@github.com/Linah-AI/A.git HEAD:claude/elegant-euler-8X2Ia`
  ثم `git update-ref refs/remotes/origin/claude/elegant-euler-8X2Ia <sha>` لإسكات الـhook.
- 🔒 **أمان حرِج:** أي توكن يُرسله المستخدم في الشات = **مكشوف**. استخدمه **مرة واحدة** فقط، **لا تحفظه أبداً** في git/commit/كود/config، وبعد الدفع **ذكّر المستخدم فوراً بإلغائه** (GitHub → Settings → Developer settings → Personal access tokens).
- 🔒 **لا تنشئ Pull Request** إلا بطلب صريح.
- 🔒 **معرّف النموذج** (claude-opus-...) يبقى في الشات فقط — **ممنوع** ظهوره في الكوميتات/الـPR/التعليقات/الكود.

### آخر حالة git (أحدث كوميت)
`1845cde` — "إصلاح تكرار الأسئلة بين الجلسات: معرّف لاعب ثابت بدل ربطه برمز الجلسة" (مدفوع ✓).

أهم كوميتات هذه الدفعة (الأقدم → الأحدث):
- `b10e8fc` إصلاح منطق زر خطأ: انتقال فوري للفريق الثاني + تفعيل صح/خطأ في مرحلة stolen.
- `ec2383a` إضافة خيار الانتقال الآلي/اليدوي بين الأسئلة في إعدادات الجولة.
- `b2744a6` تعدّد صندوق العربي الصحيح + قرار "يجيب/يمرر" عند السرقة + إصلاح z-index الشريط + إصلاح رسائل اللاعب الخاسر.
- `d0d937f` → `c4deb5a` عكس آلية إنشاء الجلسة: **التلفزيون يُنشئ** (`tv:create`) و**المضيف يلتحق** (`host:claim`) بمسح باركوده الخاص، بدل العكس.
- `b581725` تصحيح رابط الصفحة الرئيسية الذي بقي يشير لـ`host.html` القديم بعد الانعكاس أعلاه.
- `1845cde` إصلاح تكرار الأسئلة بين الجلسات (`playerId` ثابت بدل `guest:CODE`).

---

## 9) سجل القرارات والدروس (لتجنّب تكرار الأخطاء)
- ✅ أرقام إنجليزية فقط (كان فيه أرقام هندية).
- ✅ خط Baloo عريض (800) للعناوين بدل الخطوط النحيفة (Tajawal/Rakkas كانت نحيفة) — ثم حُذف Tajawal كلياً.
- ✅ شبكة حروف: من خلية عسل سداسية متداخلة بحروف ناقصة → شبكة 5×5 منتظمة، 25 حرفاً عشوائياً من 28.
- ✅ عدّاد طيف: من تدرّج لوني غريب → ألوان مسطّحة وإبرة سميكة (يناسب pop-style).
- ✅ كل الإيموجيات الزخرفية → أيقونات SVG.
- ✅ زر "افتح شاشة التلفزيون" تلقائي بدل تعليمة "افتح tv.html يدوياً" (كانت UX سيئة).
- ✅ شاشة المضيف: شعار يعود للرئيسية + إخفاء سقف العيدية عند "هدية" + خانة عدد اللاعبين.
- ✅ نشر fault-tolerant: `soon.html` للألعاب غير الجاهزة.
- 🚫 لا تبنِ تصاميم لم تُطلب (حادثة استبدال index.html بتصميم "مسرح").
- ✅ `judgeCorrect`/`judgeWrong` كانا يرفضان الحكم في مرحلة `stolen` (يقبلان `buzzed` فقط) → السبب الجذري لزر "خطأ" المعطّل.
- ✅ عند السرقة: `buzzedBy` كان يُصفَّر (`null`) فيفصل واجهة المضيف عن الفريق الثاني بلا buzz جديد → الآن يُعيَّن مباشرة لفريق السرقة (`buzzedBy = other`).
- ✅ أُضيف فاصل قرار صريح للفريق الذي انتقل له السؤال: المضيف يختار "سيجيب" (يبدأ العدّاد) أو "يمرر" (يحترق) — العدّاد لا يبدأ تلقائياً عند السرقة.
- ✅ جمع "صندوق" العربي يحتاج 4 حالات (1/2/3-10/11+) — دالة `giftPhrase(n)` مكرّرة في `host.html`/`tv.html` (لا وحدة منطق مشتركة غير `arcado-client.js`).
- ✅ شاشة اللاعب الخاسر كانت تعرض "أجبتم صح!" حتى لو الفريق الآخر هو من أجاب → صُحّح بمطابقة `s.buzzedBy === team`.
- ✅ **انعكاس معماري مهم:** أول حل لمشكلة "صفحتان على نفس الجهاز" كان خاطئاً (المضيف يُنشئ ويولّد باركود **للتلفزيون**) — المستخدم صحّح الاتجاه: التلفزيون يبقى على جهازه ويُنشئ الجلسة، والمضيف (على جهاز مستقل) يمسح باركوده الخاص. النمط الصحيح: **الشاشة التي لا يجب أن تتغيّر (التلفزيون) هي من تُنشئ؛ الشاشة المتنقّلة (المضيف) هي من تلتحق**.
- ✅ بعد الانعكاس، نسي الإصلاح تحديث `index.html` الذي بقي يربط زر اللعبة بـ`host.html` القديم (صفحة بلا جلسة ولا تعليمات) → **درس:** أي تغيير في "نقطة الدخول" لتدفّق ما يجب أن يُتبع بفحص كل الروابط الثابتة التي تفترض نقطة الدخول القديمة.
- ✅ الأسئلة كانت تتكرر بين كل جلسة وأخرى لأن `playerId = guest:${code}` يتغيّر مع كل رمز جلسة عشوائي جديد، فتصفّر ذاكرة `seen`/`wrong` كل مرة → صُحّح إلى `playerId = 'guest'` ثابت، فتستمر الذاكرة عبر الجلسات حتى ينفد البنك (منطق إعادة التدوير بأولوية الأسئلة الخاطئة في `qbank.js` كان صحيحاً من الأساس، المشكلة في تحديد الهوية فقط).
- 📌 **قاعدة ثابتة من المستخدم:** أي تعديل/إصلاح يُنفّذ في الكود يجب أن يُسجَّل في هذا الملف (خريطة الملفات + سجل القرارات + آخر كوميت) في نفس الجلسة — لا تُؤجَّل التوثيق.

---

## 10) المهام المفتوحة المحتملة (لم تُطلب بعد — للسياق فقط)
- بناء الألعاب الثلاث الباقية بالوقت الفعلي (حروف/طيف/ديكربتو) — حالياً مكابات/شاشات قريباً.
- ربط License Key الحقيقي من سلة بـ `playerId` (البنية جاهزة).
- استبدال `data/progress.json` بقاعدة بيانات حقيقية للإنتاج.
- ترقية Render لخطة مدفوعة (لإلغاء النوم).
- تنظيف الملفات القديمة (mockups) إن رغب المستخدم.

---

## 11) كيف تشغّل محلياً
```bash
npm install
npm start
# 1) افتح التلفزيون أولاً (هو من يُنشئ الجلسة): http://localhost:3000/tv.html
# 2) المضيف يمسح باركوده من الشاشة، أو يفتح: http://localhost:3000/host.html ويكتب الرمز
# 3) اللاعب يمسح باركود فريقه من شاشة التلفزيون
```
