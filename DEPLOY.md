# نشر أركيدو 🚀

اللعبة الجاهزة: **«من سيربح الجائزة»** (وقت فعلي كامل). بقية الألعاب تعرض صفحة «قريباً» تلقائياً.

## الخيار 1: تجربة محلية على شبكتك (الأسرع)

```bash
npm install
npm start
```

- **التلفزيون/المضيف:** افتح على الكمبيوتر `http://localhost:3000/`
- **جوالات اللاعبين:** لازم تكون على **نفس شبكة الواي‑فاي**. اكتشف IP جهازك:
  - ويندوز: `ipconfig` (مثال: `192.168.1.5`)
  - ماك/لينكس: `ifconfig | grep inet`
- ثم افتح المضيف عبر `http://192.168.1.5:3000/host.html` — الباركود سيحمل نفس الـ IP تلقائياً، فالجوالات تمسح وتتصل مباشرة.

> الباركود يستخدم `location.origin`، أي يأخذ العنوان الذي تفتح منه الصفحة تلقائياً — لا حاجة لأي تعديل يدوي.

## الخيار 2: نشر عام (أي جوال بأي مكان) — مجاني

### Render.com (موصى به — يدعم WebSocket مجاناً)
1. ارفع المشروع إلى GitHub.
2. على [render.com](https://render.com): **New → Web Service** واربط المستودع.
3. Render يقرأ `render.yaml` تلقائياً (Build: `npm install` · Start: `npm start`).
4. بعد دقائق يعطيك رابطاً مثل `https://arcado.onrender.com` — افتحه، وكل الباركود سيحمل هذا الدومين تلقائياً.

### بدائل تدعم WebSocket
- **Railway.app** · **Fly.io** · أي VPS فيه Node ≥ 20.

## ملاحظات
- `data/progress.json` (ذاكرة اللاعبين) يُنشأ تلقائياً وقت التشغيل. على الاستضافات المجانية ذات القرص المؤقت قد يُعاد ضبطه عند إعادة النشر — للإنتاج استخدم قرصاً دائماً أو قاعدة بيانات.
- فحص الصحّة: `GET /healthz`.
